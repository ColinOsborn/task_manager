<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>All Tasks</title>
  </head>
  <body>
    <h1>All Tasks</h1>

    <% @tasks.each do |task| %>
      <h3><% task.id %>: <a href="/tasks/<%= task.id %>"><%= task.title %></a></h3>
    <% end %>

  </body>
</html>
